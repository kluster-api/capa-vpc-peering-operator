apiVersion: v1
kind: Service
metadata:
  name: {{ include "capa-vpc-peering-operator.fullname" . }}
  labels:
    {{- include "capa-vpc-peering-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "capa-vpc-peering-operator.selectorLabels" . | nindent 4 }}
